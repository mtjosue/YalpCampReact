const router = require('express').Router();

// router.use('/campgrounds', require('./campgrounds/campgrounds'));
router.use('/campgrounds', require('./campgrounds/campgrounds'));

module.exports = router;
